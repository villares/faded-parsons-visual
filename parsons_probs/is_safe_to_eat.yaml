problem_name: is_safe_to_eat

problem_description: |
  <code>is_safe_to_eat</code>
    returns true if the seafood is safe to eat:
    either the type is "mollusk" or it's been frozen for at least 7 days.

  <pre><code>
  >>> is_safe_to_eat("tuna", 3)
  False
  >>> is_safe_to_eat("salmon", 6)
  False
  >>> is_safe_to_eat("salmon", 7)
  True
  >>> is_safe_to_eat("mollusk", 1)
  True
  >>> is_safe_to_eat("mollusk", 9)
  True
  </code></pre>

code_lines: |
  def is_safe_to_eat(seafood_type, days_frozen): #0given
  is_mollusk = !BLANK
  frozen_enough = !BLANK
  return is_mollusk !BLANK frozen_enough

test_fn: is_safe_to_eat