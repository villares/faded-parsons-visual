problem_name: lifetime_supply_calculator

problem_description: |
  <code>lifetime_supply_calculator</code>
    returns the amount of items consumed over a lifetime
    (with a max age of 100 assumed) based on the current age
    and the number consumed per day.

  <pre><code>
  >>> calculate_lifetime_supply(99, 1) # (100 - 1) * (1 * 365)
  365
  >>> calculate_lifetime_supply(99, 2) # (100 - 1) * (2 * 365)
  730
  >>> calculate_lifetime_supply(36, 3) # (100 - 36) *  (3 * 365)
  70080
  </code></pre>

code_lines: |
  def calculate_lifetime_supply(age, num_per_day): #0given
  total = (100 - !BLANK) * (!BLANK)
  return !BLANK

test_fn: calculate_lifetime_supply