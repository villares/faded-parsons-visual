problem_name: k_in_num

problem_description: |
  Complete <code>k_in_num</code>, a function which returns <code>True</code> if
  <code>num</code> has the digit <code>k</code> and returns <code>False</code>
  if <code>num</code> does not have the digit <code>k</code>. 0 is considered
  to have no digits.<br>
  <pre><code>
  >>> k_in_num(3, 123)
  True
  >>> k_in_num(5, 123)
  False
  >>> k_in_num(0, 0)
  </code></pre>
  
code_lines: |
  def k_in_num(k, num):  #0given
  while !BLANK:
  if !BLANK == num !BLANK 10:
  return True
  num = num !BLANK 10
  return False

test_fn: k_in_num
