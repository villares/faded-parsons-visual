problem_name: círculo movendo

problem_subtitle: animação usando variável global

problem_category: [100] primeiros passos

problem_description: |
  <h6>círculo movendo</h6>
  <img src="parsons_probs/121_escopo.gif"></br>
  <code>animação usando variável global</code></br>
  Ajuste os blocos de código para obter a animação do círculo.
  Note a necessidade de se modificar a variável global x dentro
  da função draw().
  		   

code_lines: |
  x = -50 # varíavel global
  def setup(): #0given
  size(400, 400) #1given
  # desenha quadros #0given
  background(0, 0, 200) #1given
  def draw(): #0given
  circle(x, height / 2, 200) #1given
  x = x + 5	
  !BLANK x # para poder modificar x
  if x > width + 50:
  x = -50

test_fn: setup
